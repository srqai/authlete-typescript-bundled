/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { AutheleteBundledCore } from "../core.js";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { AutheleteBundledError } from "../models/errors/autheletebundlederror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { ClientUpdateApiServerList } from "../models/operations/clientupdateapi.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Update Client
 *
 * @remarks
 * Update a client.
 */
export function clientManagementClientUpdateApi(
  client: AutheleteBundledCore,
  request: operations.ClientUpdateApiRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.ClientUpdateApiResponse,
    | errors.ClientUpdateApiBadRequestError
    | errors.ClientUpdateApiUnauthorizedError
    | errors.ClientUpdateApiForbiddenError
    | errors.ClientUpdateApiInternalServerError
    | AutheleteBundledError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: AutheleteBundledCore,
  request: operations.ClientUpdateApiRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.ClientUpdateApiResponse,
      | errors.ClientUpdateApiBadRequestError
      | errors.ClientUpdateApiUnauthorizedError
      | errors.ClientUpdateApiForbiddenError
      | errors.ClientUpdateApiInternalServerError
      | AutheleteBundledError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) => operations.ClientUpdateApiRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = encodeJSON("body", payload.RequestBody, { explode: true });

  const baseURL = options?.serverURL
    || pathToFunc(ClientUpdateApiServerList[0], { charEncoding: "percent" })();

  const pathParams = {
    clientId: encodeSimple("clientId", payload.clientId, {
      explode: false,
      charEncoding: "percent",
    }),
    serviceId: encodeSimple("serviceId", payload.serviceId, {
      explode: false,
      charEncoding: "percent",
    }),
  };

  const path = pathToFunc("/api/{serviceId}/client/update/{clientId}")(
    pathParams,
  );

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: "application/json",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: baseURL ?? "",
    operationID: "client_update_api",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: baseURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["400", "401", "403", "4XX", "500", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.ClientUpdateApiResponse,
    | errors.ClientUpdateApiBadRequestError
    | errors.ClientUpdateApiUnauthorizedError
    | errors.ClientUpdateApiForbiddenError
    | errors.ClientUpdateApiInternalServerError
    | AutheleteBundledError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, operations.ClientUpdateApiResponse$inboundSchema),
    M.jsonErr(400, errors.ClientUpdateApiBadRequestError$inboundSchema),
    M.jsonErr(401, errors.ClientUpdateApiUnauthorizedError$inboundSchema),
    M.jsonErr(403, errors.ClientUpdateApiForbiddenError$inboundSchema),
    M.jsonErr(500, errors.ClientUpdateApiInternalServerError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
